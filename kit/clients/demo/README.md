# Client folder

1. Land files in `raw/`
2. Edit `catalog/drivers.csv` in VisiData
3. Capture cleaning in `recipes/<source_shape>/NOTES.md`
4. Build into `warehouse/` then ship `out/`
5. Read run reports in `runs/`
