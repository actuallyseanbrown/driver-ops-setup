# Recipe — <source_shape>

Status: `stub` (NOTES only) | `mapped`

When promoted, put Polars (or YAML map + shared helpers) here or in `recipe.py`.
Inputs: files matching catalog `source_files_glob`.
Output columns: forecasting drivers schema (see kit docs).
