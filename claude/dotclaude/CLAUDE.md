# ~/.claude/CLAUDE.md — Sean global defaults

- Work on WSL files under `$HOME`, not `/mnt/c` (performance).
- Prefer `uv` for Python when available.
- Never commit client raw extracts or PII.
- Prefer small diffs; explain how to run what you write.
- For driver-ops repos, defer to project CLAUDE.md / AGENTS.md.
